# PR22 FOCUSED ACCEPTANCE RE-AUDIT

VERDICT: **A — ACCEPT**

Audited HEAD: `118166d91df18c025956e63c17727362805e1165`
Baseline: `603cbca03e292ae1d3bf424514fb60da6233bfc6`
Task: `PR22_FOCUSED_ACCEPTANCE_REAUDIT_118166D`

## Repository checks

- PR #22 is open, draft, unmerged.
- Actual PR HEAD exactly matches the task target.
- Baseline→target net change is limited to `index.html`, `tests/financial-integrity.test.js`, and `tests/pr22-lifecycle-integration.test.js`.
- Final PR diff contains the prior accepted 9 files plus `tests/pr22-lifecycle-integration.test.js`.
- No temporary workflow, script, README change, or unrelated asset remains in the final PR net diff.

Result: PASS.

## Required validation

GitHub Actions run `35002609606` completed successfully. Its validation step ran the required suites:

- `node tests/financial-integrity.test.js`
- `node tests/ac-calculator-pricing.test.js`
- `node tests/pr22-lifecycle-integration.test.js`
- syntax check for the new integration test
- inline `index.html` JavaScript syntax validation with `vm.Script`

The next step removed temporary implementation tooling and committed the tested production result as the audited HEAD.

Result: PASS.

## Focused lifecycle acceptance

`tests/pr22-lifecycle-integration.test.js` reads `index.html`, extracts the real production lifecycle helper block, executes it with `vm.runInNewContext`, and uses `window.BrunoPricingLifecycle` directly together with the real Calculator engine.

Verified by executable fixture / production wiring inspection:

- initial Catalog 100/70 + explicit Apply => Job 100/70;
- Catalog changes to 110/55 without re-Apply => existing generated Job remains 100/70;
- Margins Customer Price uses the same production customer-price helper and does not implicitly rewrite historical Job pricing;
- save/reload keeps Catalog 110/55 and historical Job 100/70;
- explicit re-Apply updates generated Job to 110/55;
- Calculate/preview does not mutate persisted Job state;
- manual material rows survive Apply/re-Apply;
- blank Catalog Your Cost deletes the explicit `yourCost` override;
- blank clear removes the persisted cost-map entry;
- Margins Your Cost uses the same blank-aware production helper;
- after clear, changing Customer Price causes Calculator fallback to resolve from the current Customer Price;
- malformed nonblank Your Cost persists as `INVALID_FINANCIAL`;
- numeric zero remains valid.

Result: PASS.

## Financial regression gate

The target changes in `tests/financial-integrity.test.js` only align Margins assertions with the shared lifecycle helpers. Existing guards remain for invalid-to-zero coercion, historical Job immutability, strict `INVALID_FINANCIAL`, valid zero, Your Cost separation, and material-cost reconciliation precedence. The required financial suites passed in run `35002609606`.

Result: PASS.

## Browser runtime

Not performed. No browser-runtime claim is made. The task-required production lifecycle helper execution, repository checks, syntax validation, and required Node suites are covered by the inspected executable integration test and successful target-producing CI run.

## Findings

```yaml
P0: []
P1: []
P2: []
```

## Final decision

```yaml
verdict: A
label: ACCEPT
audited_head: 118166d91df18c025956e63c17727362805e1165
blockers: none
production_modified: false
merge_performed: false
```

PR #22 is accepted at this exact HEAD for the focused audit scope.